Install with pip:

`pip install rosemary`

OR

`pip3 install rosemary`

Install with poetry:

`poetry add rosemary`


Requirements:
* python = "^3.11"
* pydantic = "^2.5.2"
* sqlalchemy = "^2.0.23"
* asyncpg = "^0.29.0"
* asyncio = "^3.4.3"
* logger = "^1.4"
* importlib = "^1.0.4"
* setuptools = "^69.0.2"